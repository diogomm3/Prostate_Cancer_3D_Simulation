// First open the needed libraries to execute the code

#include <stdio.h>          // Standart input output
#include <stdlib.h>         // Standart library

// Then write thebody of the main function

int main() {
    printf("Hello World!\n");

    system("pause");        // The pause command in system pauses the code running

    return 0;
}

// The function 'printf' is used to print something to the terminal

// To compile the c code use the following statment in the terminal: 'gcc .\Code.c -o Name
// This will compile the Code.c file into an executable file (extension 'exe') named Name